#include <stdio.h>
#include <assert.h> // vc faz assert(c>0) se for falso, o programa fecha e ai cabo MESMO, sem do, corta de vez, aborta

#define AREA_RET(c,l) ( (c)*(l) )

int main()  {

areaRet = AREA_RET(x+10, y+20); // ele pega aonde ta o c e faz o que pede na define, entao ele vai multiplicar o x+10 pelo y+10
areaRet = ( (x+10)*(y+20) );






  return 0;
}
